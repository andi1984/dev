<footer class="footer">
    <ol class="footer__list">
    {% for site in sites %}
        {% if 'meta/' in site.url.pathname %}
        <li class="footer__list__item">
            <a href="{{site.url}}">{{site.title}}</a>
        </li>
        {% endif %}
    {% endfor %}
        <li class="footer__list__item">
            <a href="https://amritpaldesign.com/toy-faces" target="_blank" rel="noopener noreferrer">ToyFaces</a> - Faces designed from Amrit
        </li>
    </ol>
</footer>